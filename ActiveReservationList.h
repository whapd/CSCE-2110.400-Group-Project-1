#ifndef ACTIVE_RESERVATION_LIST_H
#define ACTIVE_RESERVATION_LIST_H

// The team's linked list of active reservations is written by Dylan. Right now
// his class is called ReservationManager. All of Nate's code refers to it as
// ActiveReservationList, so if Dylan renames it (e.g. to ReservationList),
// change the ONE typedef line below and nothing else.
#include "ReservationManager.h"

typedef ReservationManager ActiveReservationList;

#endif
